### 完成度：

- 页面样式还原度较高，且实现都是通过接口方式
- 除了刷新页面没有立即显示已分组情况之外，功能全部实现

**Details:**

### 测试：

- 没有测试

**Details:**

### 知识点：

- 没有做组件拆分，只有一个 Home 组件，思考如何拆分与复用组件
- 语义化标签使用不好
- 使用了 flex 和 scss 嵌套
- 运用了 ES6+语法及 axios
- 运用 React 相关知识点，但未拆分组件，一些知识点无法验证

**Details:**

- \- 下面的 e 中可以取到 value 即为 name
- \- 没有做组件拆分，嵌套比较深，易读性低

### 工程实践：

- 有小步提交意识，但提交可以更小步，message 更明确，如拆分组件后以组件粒度等
- 没有抽出 Api 请求层
- 没有做组件拆分，Home 组件过长
- 没有组件拆分所以 scss 也没有拆分
- eslint error 较多，需要 fix

**Details:**

- \- 没有组件拆分，Home 包含内容逻辑较多，组件过长
- \- name state 比较冗余，这里不用做双向绑定
- \- 请求相关可以拆出 API 请求层，解耦请求与渲染
- \- 同 name，这里这个方法也是冗余的
- \- 加强语义化标签的使用
- \- 思考如何拆分组件，如学员列表，分组列表，和组件复用，如学员 item 等
- \- 标签 class 命名需要更加体现业务逻辑，top 之类的无法体现，且可能冲突 \*/}
- \- 不推荐使用 index 作为 key
